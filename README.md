# CS301-Project
This program will implement a file compression and decompression utility for text files. The implementation must be based on Huffman encoding. The compression program will take a plain-text file and output a compressed binary file
